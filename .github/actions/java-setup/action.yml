name: Java Setup
description: Checkout code and set up JDK 25 with Maven cache

runs:
  using: composite
  steps:
    - uses: actions/setup-java@v4
      with:
        java-version: '25'
        distribution: 'temurin'
        cache: maven
